ANTHROPIC_API_KEY=

# Optional: override the model used for API token counting.
# Defaults to claude-sonnet-5. Must be a currently available model ID —
# retired IDs make the count_tokens endpoint return 404.
ANTHROPIC_MODEL=
